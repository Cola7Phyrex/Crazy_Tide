{
  "name": "crazytide",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:pages": "vite build --base ./ --outDir docs",
    "preview": "vite preview",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "@playwright/cli": "^0.1.17",
    "vite": "^7.0.0"
  },
  "dependencies": {
    "subsetted-fonts": "^1.0.4"
  }
}
